name: parent-matrix2
on:
  push:
  workflow_dispatch:

jobs:
  #
  #  Identical to the rejected version except the uses: path is static.
  #  If this loads, the expression in uses: was the problem.
  #
  legs:
    strategy:
      fail-fast: true
      matrix:
        kind: [a, b]
    uses: ./.github/workflows/child-pass.yml
